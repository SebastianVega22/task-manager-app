PORT=4000
MONGO_URI=mongodb://localhost:27017/taskmanager
JWT_SECRET=replace-this-secret
# 0 = sin DB (stubs); 1 = conecta a Mongo usando MONGO_URI
USE_DB=0

# Notificador (valores por defecto)
DUE_NOTIFIER_CRON="*/15 * * * *"
DUE_NOTIFIER_LOOKAHEAD_HOURS=24
